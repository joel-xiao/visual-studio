{
  "name": "@visual-craft/tauri",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:app": "tauri build --bundles app",
    "build:dmg": "tauri build --bundles dmg",
    "create-dmg": "bash scripts/create-dmg.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.4"
  }
}
