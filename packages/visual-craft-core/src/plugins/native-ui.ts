import {
  create,
  NSpace,
  NSpin,
  NPopover,
  NColorPicker
} from 'naive-ui';

const components = [
  NSpace,
  NSpin,
  NPopover,
  NColorPicker
];

const naive = create({
  components
});

export default naive;
