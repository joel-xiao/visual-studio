{
  "name": "@visual-craft/electron",
  "private": true,
  "main": "dist/main/index.js",
  "type": "commonjs",
  "version": "0.0.1",
  "description": "VisualCraft - 可视化工匠 - A powerful visualization and editing tool (Electron)",
  "author": "Joel Xiao",
  "scripts": {
    "dev": "pnpm exec electron-vite dev",
    "build": "pnpm exec electron-vite build && pnpm run copy-web && pnpm exec electron-builder",
    "copy-web": "tsx scripts/copy-web.ts"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^39.2.0",
    "electron-builder": "^26.0.12",
    "electron-vite": "^4.0.1",
    "tsx": "^4.20.6",
    "vite": "^7.2.2"
  }
}
