import type { IAIContext } from '../../hooks/core/use-ai-context';

/**
 * Data Analyst Agent Apply
 * 数据通常由 layout-architect 在应用布局时一起处理
 */
export function apply(_context: IAIContext, _data: unknown): void {
  // 数据由 layout-architect 统一处理
}

