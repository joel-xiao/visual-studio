{
  "name": "@visualization-editor/web",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "npx vue-tsc --noEmit && vite build --mode web",
    "build:web": "npx vue-tsc --noEmit && vite build --mode web",
    "build:electron": "npx vue-tsc --noEmit && vite build --mode electron",
    "build:tauri": "npx vue-tsc --noEmit && vite build --mode tauri",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0",
    "axios": "^1.13.2",
    "naive-ui": "^2.43.1",
    "pinia": "^3.0.4",
    "pinia-plugin-persist": "^1.0.0",
    "pug": "^3.0.3",
    "rollup-plugin-copy": "^3.5.0",
    "sass": "^1.94.0",
    "vfonts": "^0.0.3",
    "vite-plugin-compression": "^0.5.1",
    "vite-plugin-vue-devtools": "^8.0.3",
    "vue": "^3.5.24",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.4",
    "@vitejs/plugin-vue": "^6.0.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.2",
    "vue-tsc": "^3.1.3"
  }
}
